Group uncommitted changes by topic and create separate commits for each group, then push.

### Step 1: Analyze Changes
Run `git status` and `git diff --stat HEAD` to see all modified/untracked files.

### Step 2: Group by Topic
Classify each changed file into a topic group:

**Grouping rules (in priority order):**
1. **Components** — `src/app/components/*`
2. **Pages** — `src/app/pages/*`
3. **Routing** — `src/app/routes.tsx`
4. **Styles** — `src/styles/*`
5. **Config** — `package.json`, `vite.config.ts`, `postcss.config.mjs`, `tsconfig.json`
6. **Docs** — `docs/*`, `*.md` (excluding CLAUDE.md/.claude/)
7. **Project Config** — `CLAUDE.md`, `.claude/*`, `.gitignore`

If files are tightly coupled for one logical change, group them together.

### Step 3: Present Plan
Show the proposed commit groups:

```
Group 1: [topic] — N files
  - file1.tsx
  - file2.tsx
  Commit message: "type(scope): description"

Group 2: [topic] — N files
  - file3.tsx
  Commit message: "type(scope): description"
```

Wait for user approval before proceeding.

### Step 4: Commit Each Group
For each approved group, in dependency order:
1. `git add <specific files>`
2. `git commit -m "<message>"`

**Commit message format:**
- Prefix: `feat:`, `fix:`, `refactor:`, `docs:`, `chore:`, `test:`
- Include scope: `feat(auth): add JWT refresh token`
- First line under 72 chars
- Add body if needed for context
- NEVER include "Co-Authored-By", "Generated by", or any AI/Claude signature

### Step 5: Push
```bash
git push
```

### Rules
- NEVER use `git add -A` or `git add .` — always add specific files
- NEVER commit `.env`, `node_modules/`, or `logs/`
- Never include AI attribution lines in commit messages
- If unsure about grouping, ask the user
- If there are merge conflicts or hook failures, stop and report
